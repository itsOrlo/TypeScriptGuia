let firstName = 'Tony';

// NO USAR VAR
// const = valor constante. (No cambia)
// let = valor cambiante.
// + rápidas | + ligeras

firstName = 'Juan';
const lastName = 'Stark';

console.log({firstName, lastName});


// Uso de comillas invertidas ``
console.log(`${2+5} dólares tiene ${firstName} ${lastName}`);

const final = `${2+5} dólares tiene ${firstName} ${lastName}`;

console.log(final);